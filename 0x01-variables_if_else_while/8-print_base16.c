#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 18; i < 28; i++)
	{
		putchar(i);
	}

	for (i = 63; i < 69; i++)
	{
		putchar(i);
	}

	putchar('\n');
	return (0);
}

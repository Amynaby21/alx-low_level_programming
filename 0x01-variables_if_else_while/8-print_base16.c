#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}

	for (i = 20; i < 26; i++)
	{
		putchar(i);
	}

	putchar('\n');
	return (0);
}

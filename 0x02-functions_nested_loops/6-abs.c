#include "main.h"
/**
 * _abs - Checks for the absolute value of an interger
 *
 * @i: The integer to be checked
 *
 * Return: The absolute value
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}

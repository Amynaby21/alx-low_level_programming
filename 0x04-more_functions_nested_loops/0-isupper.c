#include "main.h"

/**
 * _isupper - Checks for a uppercase character
 * @c: alphabet letter tested
 * Return: 1 or 0
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
